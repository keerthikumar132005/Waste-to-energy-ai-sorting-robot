class CameraSensor:
    def capture_image(self, image_path="captured_image.jpg"):
        # Simulation: Capture image and save it
        # In real-world, it would interact with the camera hardware.
        print(f"Capturing image and saving to {image_path}")
        return image_path

# Usage example:
# camera = CameraSensor()
# image_path = camera.capture_image()
